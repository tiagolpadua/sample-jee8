<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>org.timsoft</groupId>
	<artifactId>SampleJEE8</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>war</packaging>

	<properties>
		<!-- Java EE 8 APIs (javax.*), but compiled with / for JDK 21 -->
		<maven.compiler.release>21</maven.compiler.release>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<failOnMissingWebXml>false</failOnMissingWebXml>

		<swagger.version>2.2.55</swagger.version>
		<swagger-ui.version>5.32.14</swagger-ui.version>

		<spotless.version>3.10.2</spotless.version>
		<google-java-format.version>1.36.1</google-java-format.version>

		<lombok.version>1.18.48</lombok.version>
		<maven-compiler-plugin.version>3.14.0</maven-compiler-plugin.version>

		<h2.version>2.3.232</h2.version>
		<!-- 2.7.x is the last javax.persistence line; only used to run JPA in tests -->
		<eclipselink.version>2.7.15</eclipselink.version>
		<junit.version>5.11.4</junit.version>
		<mockito.version>5.14.2</mockito.version>
		<jacoco.version>0.8.12</jacoco.version>
		<maven-surefire-plugin.version>3.5.4</maven-surefire-plugin.version>
		<!-- last javax.ws.rs line of Jersey; test-only, to get a JAX-RS RuntimeDelegate -->
		<jersey.version>2.46</jersey.version>

		<slf4j.version>2.0.19</slf4j.version>
		<!-- Jackson was only ever on the classpath transitively, via swagger-jaxrs2. Now that
		     org.timsoft.api.json uses it directly (databind, datatype-jsr310, jaxrs-json-provider),
		     it's a real direct dependency - pin it as one instead of relying on what Swagger happens
		     to pull in. -->
		<jackson.version>2.22.1</jackson.version>
	</properties>

	<dependencies>
		<dependency>
			<groupId>javax</groupId>
			<artifactId>javaee-api</artifactId>
			<version>8.0.1</version>
			<scope>provided</scope>
		</dependency>

		<!-- Compile-time only: not bundled in the WAR, not exported transitively -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<version>${lombok.version}</version>
			<scope>provided</scope>
			<optional>true</optional>
		</dependency>

		<!-- OpenAPI 3 spec generation from JAX-RS annotations (javax build) -->
		<dependency>
			<groupId>io.swagger.core.v3</groupId>
			<artifactId>swagger-jaxrs2</artifactId>
			<version>${swagger.version}</version>
		</dependency>

		<!-- Swagger UI static assets, served from META-INF/resources/webjars/... -->
		<dependency>
			<groupId>org.webjars</groupId>
			<artifactId>swagger-ui</artifactId>
			<version>${swagger-ui.version}</version>
		</dependency>

		<!-- H2 JDBC driver - bundled in the WAR (WebLogic has no in-memory DB we want) -->
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<version>${h2.version}</version>
		</dependency>

		<!-- Direct Jackson dependencies for org.timsoft.api.json: registers Jackson as the
		     JAX-RS JSON provider so it wins over WebLogic's default JSON-B/MOXy selection,
		     with LocalDateTime support - see CLAUDE.md. -->
		<dependency>
			<groupId>com.fasterxml.jackson.jaxrs</groupId>
			<artifactId>jackson-jaxrs-json-provider</artifactId>
			<version>${jackson.version}</version>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.core</groupId>
			<artifactId>jackson-databind</artifactId>
			<version>${jackson.version}</version>
		</dependency>
		<dependency>
			<groupId>com.fasterxml.jackson.datatype</groupId>
			<artifactId>jackson-datatype-jsr310</artifactId>
			<version>${jackson.version}</version>
		</dependency>

		<!-- SLF4J facade (used via Lombok @Slf4j) + a JUL binding, so log calls still land
		     in WebLogic's server log exactly like the java.util.logging calls they replace. -->
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>${slf4j.version}</version>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-jdk14</artifactId>
			<version>${slf4j.version}</version>
		</dependency>

		<!-- ===== test only ===== -->

		<!-- JPA provider for tests only; WebLogic supplies EclipseLink at runtime.
		     Exclude its bundled javax.persistence API - javaee-api already provides it. -->
		<dependency>
			<groupId>org.eclipse.persistence</groupId>
			<artifactId>eclipselink</artifactId>
			<version>${eclipselink.version}</version>
			<scope>test</scope>
			<exclusions>
				<exclusion>
					<groupId>org.eclipse.persistence</groupId>
					<artifactId>jakarta.persistence</artifactId>
				</exclusion>
				<exclusion>
					<groupId>org.eclipse.persistence</groupId>
					<artifactId>javax.persistence</artifactId>
				</exclusion>
			</exclusions>
		</dependency>

		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>${junit.version}</version>
			<scope>test</scope>
		</dependency>

		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-junit-jupiter</artifactId>
			<version>${mockito.version}</version>
			<scope>test</scope>
		</dependency>

		<!-- Provides a javax.ws.rs RuntimeDelegate so Response/UriBuilder work in
		     plain unit tests (no container). Matches the Jersey WebLogic ships. -->
		<dependency>
			<groupId>org.glassfish.jersey.core</groupId>
			<artifactId>jersey-common</artifactId>
			<version>${jersey.version}</version>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Point java.util.logging at the test config (quiets the ExceptionMapper
			     stack traces that the mapper tests trigger on purpose). -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>${maven-surefire-plugin.version}</version>
				<configuration>
					<systemPropertyVariables>
						<java.util.logging.config.file>${project.basedir}/src/test/resources/logging.properties</java.util.logging.config.file>
					</systemPropertyVariables>
					<!-- @{argLine} keeps JaCoCo's agent; the flag silences Mockito/ByteBuddy's
					     "Java agent loaded dynamically" warning on JDK 21. -->
					<argLine>@{argLine} -XX:+EnableDynamicAgentLoading</argLine>
				</configuration>
			</plugin>

			<!-- Test coverage: `jacoco:check` runs in `verify` and fails the build
			     below 70% line coverage. HTML report at target/site/jacoco/.
			     Lombok-generated members are skipped (lombok.addLombokGeneratedAnnotation). -->
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<version>${jacoco.version}</version>
				<configuration>
					<excludes>
						<!-- Framework glue: not unit-testable without a container -->
						<exclude>org/timsoft/api/ApplicationConfig.class</exclude>
						<exclude>org/timsoft/api/persistence/EntityManagerProducer.*</exclude>
						<exclude>org/timsoft/api/persistence/TransactionalInterceptor.*</exclude>
						<exclude>org/timsoft/api/persistence/Tx.*</exclude>
						<exclude>org/timsoft/api/ping/**</exclude>
						<exclude>org/timsoft/api/diagnostics/**</exclude>
					</excludes>
				</configuration>
				<executions>
					<execution>
						<id>jacoco-prepare-agent</id>
						<goals>
							<goal>prepare-agent</goal>
						</goals>
					</execution>
					<execution>
						<id>jacoco-report</id>
						<phase>test</phase>
						<goals>
							<goal>report</goal>
						</goals>
					</execution>
					<execution>
						<id>jacoco-check</id>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
						<configuration>
							<rules>
								<rule>
									<element>BUNDLE</element>
									<limits>
										<limit>
											<counter>LINE</counter>
											<value>COVEREDRATIO</value>
											<minimum>0.70</minimum>
										</limit>
									</limits>
								</rule>
							</rules>
						</configuration>
					</execution>
				</executions>
			</plugin>

			<!-- Run Lombok as an explicit annotation processor (not off the
			     classpath), so it keeps working on newer JDKs. -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>${maven-compiler-plugin.version}</version>
				<configuration>
					<annotationProcessorPaths>
						<path>
							<groupId>org.projectlombok</groupId>
							<artifactId>lombok</artifactId>
							<version>${lombok.version}</version>
						</path>
					</annotationProcessorPaths>
				</configuration>
			</plugin>

			<!-- Code formatting gate: `spotless:check` runs in process-sources,
			     so `mvn compile/package/install` fails on badly formatted code.
			     Run `mvn spotless:apply` to fix formatting. -->
			<plugin>
				<groupId>com.diffplug.spotless</groupId>
				<artifactId>spotless-maven-plugin</artifactId>
				<version>${spotless.version}</version>
				<configuration>
					<java>
						<googleJavaFormat>
							<version>${google-java-format.version}</version>
						</googleJavaFormat>
						<removeUnusedImports />
						<formatAnnotations />
					</java>
				</configuration>
				<executions>
					<execution>
						<id>spotless-check</id>
						<phase>process-sources</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
